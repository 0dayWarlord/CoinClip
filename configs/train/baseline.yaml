# Baseline training configuration

batch_size: 32
eval_batch_size: 32
num_epochs: 50
learning_rate: 1e-4
weight_decay: 0.01
scheduler: "cosine"
num_workers: 4
device: "cuda"
seed: 42
checkpoint_dir: "checkpoints"
log_dir: "logs"
save_best_metric: "val_loss"

